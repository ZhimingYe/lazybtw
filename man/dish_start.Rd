% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dish.R
\name{dish_start}
\alias{dish_start}
\title{Start a local lazybtw dish session}
\usage{
dish_start(
  session_name = NULL,
  project = getwd(),
  max_entries = getOption("lazybtw.dish.max_entries", 100L),
  max_entry_bytes = getOption("lazybtw.dish.max_entry_bytes", 64L * 1024L),
  max_total_bytes = getOption("lazybtw.dish.max_total_bytes", 5L * 1024L * 1024L),
  cleanup = TRUE
)
}
\arguments{
\item{session_name}{Optional stable session name. If \code{NULL}, lazybtw
generates a unique session id.}

\item{project}{Project directory recorded in session metadata.}

\item{max_entries}{Maximum number of dish entries to retain per session.}

\item{max_entry_bytes}{Maximum UTF-8 bytes to retain for a single dish entry.}

\item{max_total_bytes}{Maximum bytes to retain across the dish file.}

\item{cleanup}{Whether to remove dead/stale lazybtw sessions before starting
the new session.}
}
\description{
Start a local lazybtw dish session
}
